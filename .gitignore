# Local Cloudflare / Wrangler
.wrangler
_worker.bundle
.dev.vars
CLAUDE.md
# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.*.local

# Local data / secrets
db.json

# 脚本运行产物（kb-selftest.js 生成的明细，每次跑都会覆盖）
scripts/selftest-hits.json

# Logs
*.log
npm-debug.log*

# OS files
.DS_Store
Thumbs.db

# Editor directories
.vscode/
.idea/
*.swp
*.swo

# AI 工作记忆（个人化，不上传）
.workbuddy/
# 另一 AI 工具写入的巡检记忆，与 .workbuddy/ 并存易分叉，故一并忽略
.workbuddy-ai/
.superdesign/
# 本地 Agent 配置
.agents/
# AI 识图调试工具运行缓存（.dsh 工具生成，非项目文件）
.dsh-vision-toolkit/

# 内部文档与选型笔记：不进公开发布面
# （Pages 以仓库根为发布根 —— 只要被 git 跟踪就公网可下载；这些文件运行时无人读取）
/docs/
/model-swap-options-and-free-platforms.md
# ⚠️ 不要加 Markdown/kb.md：它是运行时依赖（functions/api/chat.js 启动时
#    fetch('/Markdown/kb.md')）。撤下后该路径会返回软 404 的首页 HTML，而
#    loadKnowledgeBase 不做内容校验就缓存它 → 全部提问静默降级为通用模式。